package server.agents.field;

/** Capability-shaped formation role; it is deliberately not a job-class enum. */
public enum AgentFieldRole {
    VANGUARD,
    RANGED_HOLDER,
    AOE_CLEARER,
    ROAMER,
    SUPPORT,
    RESERVE
}
