/**
 * Read-only Agent catalog loader and query package.
 *
 * <p>This package loads generated catalog JSON artifacts into immutable query
 * indexes. Decision catalogs may be sampled beside live Agent ticks for
 * comparison, but catalog data never executes capability actions or bypasses
 * runtime validation.</p>
 */
package server.agents.catalog;
