package server.agents.capabilities.townlife;

/** Rendering and movement detail used by the same TownLife state machine. */
public enum AgentTownLifeFidelity {
    PRESENTATION,
    BACKGROUND_ACTIVE,
    BACKGROUND_ABSTRACT
}
