package server.agents.capabilities.townlife;

/** Controls how a town may admit Agents without changing the local TownLife engine. */
public enum AgentTownLifeAdmissionMode {
    DISABLED,
    MANUAL_ONLY,
    AMBIENT
}
