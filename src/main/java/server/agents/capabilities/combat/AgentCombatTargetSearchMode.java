package server.agents.capabilities.combat;

/** High-level intent used by quest grind target acquisition. */
public enum AgentCombatTargetSearchMode {
    LOCAL_CLEAR,
    REGION_HARVEST,
    MAP_WIDE_RECOVERY,
    SPAWN_PRESSURE
}
