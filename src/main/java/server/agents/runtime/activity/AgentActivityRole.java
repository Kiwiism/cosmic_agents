package server.agents.runtime.activity;

/** Architectural role of one controller registered with the Activity Host. */
public enum AgentActivityRole {
    PRIMARY,
    SUPPORT,
    COMPATIBILITY,
    CAPABILITY
}
