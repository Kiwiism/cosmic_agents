package server.agents.runtime.activity.world;

/** UNKNOWN prevents the Director from treating an unobservable fact as false. */
public enum AgentWorldMilestoneStatus {
    ACHIEVED,
    NOT_ACHIEVED,
    UNKNOWN
}
