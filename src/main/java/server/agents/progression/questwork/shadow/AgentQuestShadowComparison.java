package server.agents.progression.questwork.shadow;

/** Exact comparison categories; shadow mode deliberately makes no fuzzy equivalence claim. */
public enum AgentQuestShadowComparison {
    MATCH,
    ACTION_MISMATCH,
    DESTINATION_MISMATCH,
    QUEST_MISMATCH
}
